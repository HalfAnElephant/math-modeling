# 02 核心模型与归一化目标子计划

> **目标：** 实现无人机路线评价、方案汇总、负载指标和归一化多目标评分，避免不同量纲指标混合导致评分失真。

**前置条件：**

```bash
cd "/Users/xcy/Documents/100 项目/数学建模"
python3 -m pytest tests/test_package.py tests/test_data.py -q
```

预期：全部通过。

## 1. 创建路线指标测试

创建 `tests/test_model.py`，内容必须覆盖：

```python
from pathlib import Path

from c_uav_inspection.data import load_problem_data
from c_uav_inspection.model import UAVRoute, evaluate_uav_route, summarize_uav_solution


DATA_PATH = Path("2026同济数学建模竞赛赛题/2026C数据.xlsx")


def test_evaluate_single_base_route_matches_manual_energy_formula():
    data = load_problem_data(DATA_PATH)
    route = UAVRoute(uav_id=1, sortie_id=1, node_sequence=(0, 1, 0), hover_times_s={1: 50})

    metrics = evaluate_uav_route(route, data)

    expected_energy = data.flight_energy_j[(0, 1)] + data.flight_energy_j[(1, 0)] + 50 * data.params.hover_power_j_per_s
    expected_time = data.flight_time_s[(0, 1)] + data.flight_time_s[(1, 0)] + 50
    assert metrics.energy_j == expected_energy
    assert metrics.duration_s == expected_time
    assert metrics.feasible_energy is True


def test_summarize_solution_includes_swap_time_between_sorties():
    data = load_problem_data(DATA_PATH)
    routes = (
        UAVRoute(uav_id=1, sortie_id=1, node_sequence=(0, 1, 0), hover_times_s={1: 50}),
        UAVRoute(uav_id=1, sortie_id=2, node_sequence=(0, 3, 0), hover_times_s={3: 35}),
        UAVRoute(uav_id=2, sortie_id=1, node_sequence=(0, 4, 0), hover_times_s={4: 55}),
    )

    summary = summarize_uav_solution(routes, data, battery_swap_time_s=300)

    uav1_route_time = sum(evaluate_uav_route(route, data).duration_s for route in routes if route.uav_id == 1)
    uav2_route_time = sum(evaluate_uav_route(route, data).duration_s for route in routes if route.uav_id == 2)
    assert summary.uav_work_times_s[1] == uav1_route_time + 300
    assert summary.uav_work_times_s[2] == uav2_route_time
    assert summary.uav_phase_time_s == max(summary.uav_work_times_s.values())
```

运行：

```bash
python3 -m pytest tests/test_model.py -q
```

预期：失败，错误包含 `No module named 'c_uav_inspection.model'`。

## 2. 实现路线评价模块

创建 `c_uav_inspection/model.py`，必须包含：

- `UAVRoute`
  - `uav_id: int`
  - `sortie_id: int`
  - `node_sequence: tuple[int, ...]`
  - `hover_times_s: Mapping[int, float]`

- `RouteMetrics`
  - `duration_s`
  - `energy_j`
  - `feasible_energy`

- `UAVSolutionSummary`
  - `uav_phase_time_s`
  - `total_energy_j`
  - `uav_work_times_s`
  - `load_std_s`
  - `feasible_energy`

- `evaluate_uav_route(route, data)`
  - 飞行时间：沿 `node_sequence` 累加 `data.flight_time_s[(source, target)]`。
  - 飞行能耗：沿 `node_sequence` 累加 `data.flight_energy_j[(source, target)]`。
  - 悬停时间：`sum(route.hover_times_s.values())`。
  - 悬停能耗：悬停时间乘 `data.params.hover_power_j_per_s`。
  - `feasible_energy`：总能耗不超过 `data.params.effective_energy_limit_j`。

- `summarize_uav_solution(routes, data, battery_swap_time_s)`
  - 按 `uav_id` 分组。
  - 每架无人机工作时长等于路线时长之和 + `(sortie_count - 1) * battery_swap_time_s`。
  - `uav_phase_time_s` 是所有无人机工作时长最大值。
  - `load_std_s` 使用总体标准差；只有 0 或 1 架无人机时为 0。

运行：

```bash
python3 -m pytest tests/test_model.py -q
```

预期：

```text
2 passed
```

## 3. 创建归一化目标测试

创建 `tests/test_objective.py`，内容为：

```python
from c_uav_inspection.objective import ObjectiveTermBounds, bounds_from_candidates, normalize_term, weighted_normalized_objective


def test_normalize_term_maps_value_to_unit_interval():
    bounds = ObjectiveTermBounds(lower=100.0, upper=300.0)

    assert normalize_term(100.0, bounds) == 0.0
    assert normalize_term(200.0, bounds) == 0.5
    assert normalize_term(300.0, bounds) == 1.0


def test_normalize_term_handles_degenerate_bounds():
    bounds = ObjectiveTermBounds(lower=42.0, upper=42.0)

    assert normalize_term(42.0, bounds) == 0.0


def test_weighted_objective_does_not_let_large_units_dominate():
    rows = [
        {"time_s": 1000.0, "energy_j": 900000.0},
        {"time_s": 2000.0, "energy_j": 100000.0},
    ]
    bounds = bounds_from_candidates(rows, ("time_s", "energy_j"))

    score = weighted_normalized_objective(
        values={"time_s": 1000.0, "energy_j": 900000.0},
        bounds=bounds,
        weights={"time_s": 0.5, "energy_j": 0.5},
    )

    assert score == 0.5
```

运行：

```bash
python3 -m pytest tests/test_objective.py -q
```

预期：失败，错误包含 `No module named 'c_uav_inspection.objective'`。

## 4. 实现归一化目标模块

创建 `c_uav_inspection/objective.py`，必须包含：

- `ObjectiveTermBounds(lower, upper)`
- `normalize_term(value, bounds)`
  - `upper <= lower` 时返回 `0.0`。
  - 正常情况返回截断到 `[0, 1]` 的 min-max 归一化值。
- `bounds_from_candidates(rows, term_names)`
  - 对每个指标从候选方案中取最小值和最大值。
- `weighted_normalized_objective(values, bounds, weights)`
  - 权重和必须大于 0。
  - 每个指标先调用 `normalize_term`，再按权重归一化求和。

运行：

```bash
python3 -m pytest tests/test_model.py tests/test_objective.py -q
```

预期：

```text
5 passed
```

## 5. 验收本子计划

运行：

```bash
python3 -m pytest tests/test_package.py tests/test_data.py tests/test_model.py tests/test_objective.py -q
```

预期：全部通过。

提交：

```bash
git add c_uav_inspection/model.py c_uav_inspection/objective.py tests/test_model.py tests/test_objective.py
git commit -m "feat: calculate route metrics and normalized objectives"
```
