# 01 环境与数据加载子计划

> **目标：** 创建 Python 包、确认测试环境可用、读取 `2026C数据.xlsx`，并完成基础数据一致性核验。

**前置条件：**

```bash
cd "/Users/xcy/Documents/100 项目/数学建模"
test -f "2026同济数学建模竞赛赛题/2026C数据.xlsx"
```

预期：命令无输出，退出码为 0。

## 1. 检查 Python 依赖

运行：

```bash
python3 - <<'PY'
import importlib
for name in ("openpyxl", "pytest", "numpy", "matplotlib"):
    importlib.import_module(name)
print("deps ok")
PY
```

预期输出：

```text
deps ok
```

如果失败，运行：

```bash
python3 -m pip install openpyxl pytest numpy matplotlib
```

## 2. 创建包导入测试

创建 `tests/test_package.py`，内容为：

```python
from c_uav_inspection import __version__


def test_package_exposes_version():
    assert __version__ == "0.1.0"
```

运行：

```bash
python3 -m pytest tests/test_package.py -q
```

预期：失败，错误包含 `ModuleNotFoundError`。

## 3. 创建包入口

创建 `c_uav_inspection/__init__.py`，内容为：

```python
"""Reproducible models and experiments for Problem C."""

__version__ = "0.1.0"
```

运行：

```bash
python3 -m pytest tests/test_package.py -q
```

预期：

```text
1 passed
```

## 4. 创建数据读取测试

创建 `tests/test_data.py`，必须包含以下测试：

```python
from pathlib import Path

from c_uav_inspection.data import load_problem_data, validate_problem_data


DATA_PATH = Path("2026同济数学建模竞赛赛题/2026C数据.xlsx")


def test_load_problem_data_counts_targets_and_params():
    data = load_problem_data(DATA_PATH)

    assert len(data.targets) == 16
    assert data.params.k_max == 4
    assert data.params.effective_energy_limit_j == 135000
    assert data.params.hover_power_j_per_s == 220
    assert data.params.battery_swap_time_s == 300
    assert data.params.operating_horizon_s == 2600


def test_loaded_targets_preserve_key_values():
    data = load_problem_data(DATA_PATH)
    by_id = {target.node_id: target for target in data.targets}

    assert by_id[1].node_name == "B1-东立面裂缝"
    assert by_id[1].base_hover_time_s == 50
    assert by_id[1].direct_confirm_time_s == 200
    assert by_id[16].priority_weight == 3
    assert by_id[16].direct_confirm_time_s == 560


def test_matrices_include_depot_and_target_pairs():
    data = load_problem_data(DATA_PATH)

    assert data.flight_time_s[(0, 1)] > 0
    assert data.flight_energy_j[(0, 16)] > 0
    assert data.ground_time_s[("P0", "MP01")] > 0
    assert data.flight_time_s[(3, 3)] == 0


def test_validate_problem_data_returns_expected_summary():
    data = load_problem_data(DATA_PATH)
    summary = validate_problem_data(data)

    assert summary["target_count"] == 16
    assert summary["base_hover_sum_s"] == 790
    assert summary["direct_hover_sum_s"] == 5210
    assert summary["confirm_thresholds_valid"] is True
    assert summary["max_single_direct_confirm_energy_j"] <= data.params.effective_energy_limit_j
```

运行：

```bash
python3 -m pytest tests/test_data.py -q
```

预期：失败，错误包含 `No module named 'c_uav_inspection.data'`。

## 5. 实现数据模块

创建 `c_uav_inspection/data.py`。必须定义以下数据类和函数：

- `UAVParams`
  - `k_max`
  - `battery_capacity_j`
  - `safety_reserve_j`
  - `effective_energy_limit_j`
  - `horizontal_speed_mps`
  - `vertical_speed_mps`
  - `horizontal_energy_j_per_m`
  - `up_energy_j_per_m`
  - `down_energy_j_per_m`
  - `hover_power_j_per_s`
  - `battery_swap_time_s`
  - `operating_horizon_s`
  - `walking_speed_mps`
  - `walking_detour_factor`

- `Target`
  - `node_id`
  - `node_name`
  - `building_id`
  - `x_m`
  - `y_m`
  - `z_m`
  - `priority_level`
  - `priority_weight`
  - `issue_type`
  - `base_hover_time_s`
  - `direct_confirm_time_s`
  - `manual_point_id`
  - `manual_x_m`
  - `manual_y_m`
  - `manual_service_time_s`

- `ProblemData`
  - `params`
  - `targets`
  - `flight_time_s`
  - `flight_energy_j`
  - `ground_time_s`

- `load_problem_data(path)`
  - 读取工作表 `UAV_Params`、`NodeData`、`FlightTime`、`FlightEnergy`、`GroundTime`。
  - 保留节点 0 到 16 的飞行矩阵键 `(i, j)`。
  - 保留物业点键 `("P0", "MP01")` 这一类字符串矩阵键。

- `validate_problem_data(data)`
  - 返回一个字典，至少包含测试中使用的 5 个键。
  - `max_single_direct_confirm_energy_j` 计算公式为：从 0 往返目标点飞行能耗 + `direct_confirm_time_s * hover_power_j_per_s`。

## 6. 验收本子计划

运行：

```bash
python3 -m pytest tests/test_package.py tests/test_data.py -q
```

预期：

```text
5 passed
```

提交：

```bash
git add c_uav_inspection/__init__.py c_uav_inspection/data.py tests/test_package.py tests/test_data.py
git commit -m "feat: load c problem workbook data"
```

