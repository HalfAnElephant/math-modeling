# 06 最终验收子计划

> **目标：** 从零重建全部结果，检查约束、输出文件和论文风险点，确保交付材料可以复现。

**前置条件：**

```bash
cd "/Users/xcy/Documents/100 项目/数学建模"
python3 -m pytest tests -q
```

预期：全部通过。

## 1. 从零重建输出目录

运行：

```bash
rm -rf outputs/c_uav_inspection
python3 - <<'PY'
from pathlib import Path
from c_uav_inspection.experiments import run_all_experiments
from c_uav_inspection.plots import generate_all_figures

data_path = Path("2026同济数学建模竞赛赛题/2026C数据.xlsx")
out = Path("outputs/c_uav_inspection")
run_all_experiments(data_path, out)
generate_all_figures(data_path, out)
print("rebuild ok")
PY
```

预期输出：

```text
rebuild ok
```

## 2. 检查输出清单

运行：

```bash
python3 - <<'PY'
from pathlib import Path

expected = {
    "data_validation.json",
    "problem1_k_comparison.csv",
    "problem1_swap_sensitivity.csv",
    "problem2_k_comparison.csv",
    "problem2_threshold_sensitivity.csv",
    "recommended_solution.json",
    "problem1_k_comparison.png",
    "problem2_threshold_sensitivity.png",
    "recommended_routes.png",
}
actual = {path.name for path in Path("outputs/c_uav_inspection").iterdir() if path.is_file()}
missing = sorted(expected - actual)
extra = sorted(actual - expected)
assert not missing, missing
print("inventory ok")
print("extra files:", extra)
PY
```

预期输出包含：

```text
inventory ok
```

`extra files` 可以为空列表，也可以包含后续手工增加的补充图表；若出现临时文件，应删除。

## 3. 检查 CSV 非空和归一化列

运行：

```bash
python3 - <<'PY'
import csv
from pathlib import Path

for path in sorted(Path("outputs/c_uav_inspection").glob("*.csv")):
    with path.open("r", encoding="utf-8", newline="") as handle:
        rows = list(csv.DictReader(handle))
    assert rows, path
    assert "normalized_objective" in rows[0], path
    for row in rows:
        value = float(row["normalized_objective"])
        assert 0.0 <= value <= 1.0, (path, value)
    print(path.name, len(rows), "rows")
PY
```

预期：每个 CSV 打印至少 1 行数据，无断言失败。

## 4. 检查推荐方案基本可行性

运行：

```bash
python3 - <<'PY'
import json
from pathlib import Path

path = Path("outputs/c_uav_inspection/recommended_solution.json")
data = json.loads(path.read_text(encoding="utf-8"))
assert data["closed_loop_time_s"] > 0
assert isinstance(data["routes"], list)
assert len(data["routes"]) >= 1
for route in data["routes"]:
    assert route["node_sequence"][0] == 0
    assert route["node_sequence"][-1] == 0
    assert route["hover_times_s"]
print("recommended solution ok")
PY
```

预期输出：

```text
recommended solution ok
```

## 5. 检查文档风险表述

运行：

```bash
rg -n "TBD|TODO|直接相加|新建一趟单点|单点直接|原子化|不可分割|min F1 = alpha\\*T_u|lambda1\\*\\(T_u \\+ T_g\\)" \
  PLAN docs/superpowers/plans report c_uav_inspection tests \
  --glob '!06-最终验收.md'
```

预期：无输出。若有输出，必须改掉对应表述或代码。

## 6. 检查关键修正是否仍存在

运行：

```bash
rg -n "normalized_objective|effective_direct_threshold|split_order_into_energy_feasible_routes|solve_uav_hover_plan|direct_threshold_multiplier|closed_loop_time_s" \
  c_uav_inspection tests report PLAN
```

预期：能看到这些关键名称在代码、测试、报告和计划中出现。

## 7. 最终测试

运行：

```bash
python3 -m pytest tests -q
```

预期：全部通过。

## 8. 最终提交

运行：

```bash
git status --short
git add PLAN docs/superpowers/plans c_uav_inspection tests outputs/c_uav_inspection report
git commit -m "chore: finalize c problem execution plans and outputs"
```

如果 `git status --short` 显示与本任务无关的用户文件，先不要加入这些文件，只提交本计划和实现产生的相关路径。

## 9. 交付摘要

最终回复论文团队时使用下面这段：

```text
已完成 C 题可复现实验流程。
主模型：多无人机多趟能耗约束 VRP + 可拆分悬停时间分配 + 物业复核 TSP。
关键修正：多目标归一化评分、连续悬停拆分、直接确认重建搜索、阈值敏感性下界约束。
未引入 ML/DL。
核心输出：K 对比、换电敏感性、阈值敏感性、推荐巡检方案、路径图。
论文重点：归一化多目标评价、连续悬停时间分配、空地替代收益、边际收益递减配置建议。
```
