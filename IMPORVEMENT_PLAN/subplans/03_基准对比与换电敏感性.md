# 基准对比与换电敏感性 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补齐赛题问题2(1)要求的“仅基础巡检 vs 联合优化”对比表，并把问题1换电时间敏感性从无效的 `K=4` 改为能进入关键路径的 `K=1`。

**Architecture:** 在 `experiments.py` 中新增基准闭环评估和两组换电敏感性输出。基准闭环使用问题1基础巡检路线加全人工复核；推荐方案使用优先级修正后的问题2联合求解。换电敏感性主表固定 `K=1`，补充表保留 `K=4` 解释为何不敏感。

**Tech Stack:** Python 3.11, existing `solve_problem1_for_k`, `solve_joint_problem_for_k`, `solve_ground_tsp`, `summarize_uav_solution`, `pytest`, `csv`.

---

## 1. 涉及文件

| 类型 | 文件 |
|---|---|
| 修改 | `c_uav_inspection/experiments.py` |
| 修改 | `tests/test_experiments.py` |
| 修改 | `report/c_uav_inspection_paper.md` |

## 2. 仅基础巡检闭环基准

- [ ] **Step 1: 在 `experiments.py` 导入 `solve_ground_tsp`**

```python
from c_uav_inspection.problem2 import solve_ground_tsp, solve_joint_problem_for_k
```

如果文件中已经导入 `solve_joint_problem_for_k`，只补 `solve_ground_tsp`。

- [ ] **Step 2: 新增 `_solve_base_only_closed_loop`**

```python
def _solve_base_only_closed_loop(data: ProblemData, k: int) -> dict[str, Any]:
    sw = data.params.battery_swap_time_s
    sol = solve_problem1_for_k(data, k, sw, improve=True)
    summary = summarize_uav_solution(sol.routes, data, sw)
    manual_points = tuple(target.manual_point_id for target in data.targets)
    ground = solve_ground_tsp(data, manual_points)
    weighted_manual_cost = sum(target.priority_weight for target in data.targets)

    return {
        "scheme": "仅基础巡检(alpha->inf)",
        "k": k,
        "direct_threshold_multiplier": "inf",
        "uav_phase_time_s": summary.uav_phase_time_s,
        "ground_review_time_s": ground.total_time_s,
        "closed_loop_time_s": summary.uav_phase_time_s + ground.total_time_s,
        "manual_count": len(set(manual_points)),
        "weighted_manual_cost": weighted_manual_cost,
        "direct_confirm_count": 0,
        "total_energy_j": summary.total_energy_j,
        "load_std_s": summary.load_std_s,
    }
```

当前数据下，基础巡检行应为：

| 字段 | 值 |
|---|---:|
| `uav_phase_time_s` | 639.3318181818182 |
| `ground_review_time_s` | 3894.0 |
| `closed_loop_time_s` | 4533.331818181819 |
| `manual_count` | 16 |
| `weighted_manual_cost` | 36 |

如果子计划 01 修改了问题一主结果，此处仍可使用旧基础巡检满载解作为“仅基础巡检退化基准”；论文中要说明该基准用于问题2退化对照，不必等同于问题一时间优先推荐配置。

- [ ] **Step 3: 新增 `_run_problem2_baseline_comparison`**

```python
def _run_problem2_baseline_comparison(data: ProblemData, output_dir: Path) -> None:
    k = data.params.k_max
    sw = data.params.battery_swap_time_s
    base_row = _solve_base_only_closed_loop(data, k)

    joint = solve_joint_problem_for_k(data, k, 1.0)
    summary = summarize_uav_solution(joint.routes, data, sw)
    joint_row = {
        "scheme": "推荐方案(K=4, alpha=1.0)",
        "k": k,
        "direct_threshold_multiplier": 1.0,
        "uav_phase_time_s": joint.closed_loop.uav_phase_time_s,
        "ground_review_time_s": joint.closed_loop.ground_review_time_s,
        "closed_loop_time_s": joint.closed_loop.closed_loop_time_s,
        "manual_count": joint.closed_loop.manual_count,
        "weighted_manual_cost": joint.closed_loop.weighted_manual_cost,
        "direct_confirm_count": len(joint.closed_loop.direct_confirmed_nodes),
        "total_energy_j": summary.total_energy_j,
        "load_std_s": summary.load_std_s,
    }

    _write_csv(output_dir / "problem2_baseline_comparison.csv", [base_row, joint_row])
```

- [ ] **Step 4: 在 `run_all_experiments` 中调用**

调用位置放在问题2 K 对比之前：

```python
_run_problem2_baseline_comparison(data, output_dir)
_run_problem2_k_comparison(data, output_dir)
_run_problem2_threshold_sensitivity(data, output_dir)
```

## 3. 换电时间敏感性改为 K=1

- [ ] **Step 5: 改写 `_run_problem1_swap_sensitivity`**

将现有主输出改为 `K=1`：

```python
def _run_problem1_swap_sensitivity(data, output_dir: Path) -> None:
    rows: list[dict[str, Any]] = []
    k = 1

    for swap_s in (0, 150, 300, 450, 600):
        sol = solve_problem1_for_k(data, k, float(swap_s), improve=True)
        summary = summarize_uav_solution(sol.routes, data, float(swap_s))
        rows.append({
            "k": k,
            "battery_swap_time_s": swap_s,
            "uav_phase_time_s": summary.uav_phase_time_s,
            "total_energy_j": summary.total_energy_j,
            "load_std_s": summary.load_std_s,
            "route_count": len(sol.routes),
        })

    rows = _add_normalized_objective(rows, {
        "uav_phase_time_s": 1.0,
        "total_energy_j": 0.0,
        "load_std_s": 0.0,
    })
    _write_csv(output_dir / "problem1_swap_sensitivity_k1.csv", rows)
```

当前数据应得到：

| 换电时间/s | 阶段时间/s |
|---:|---:|
| 0 | 988.3000000000001 |
| 150 | 1138.3000000000002 |
| 300 | 1288.3000000000002 |
| 450 | 1438.3000000000002 |
| 600 | 1588.3000000000002 |

- [ ] **Step 6: 新增 K=4 参考表**

```python
def _run_problem1_swap_sensitivity_k4_reference(data, output_dir: Path) -> None:
    rows: list[dict[str, Any]] = []
    k = data.params.k_max

    for swap_s in (0, 150, 300, 450, 600):
        sol = solve_problem1_for_k(data, k, float(swap_s), improve=True)
        summary = summarize_uav_solution(sol.routes, data, float(swap_s))
        rows.append({
            "k": k,
            "battery_swap_time_s": swap_s,
            "uav_phase_time_s": summary.uav_phase_time_s,
            "total_energy_j": summary.total_energy_j,
            "load_std_s": summary.load_std_s,
            "route_count": len(sol.routes),
            "notes": "No swap on critical path when each assigned UAV flies at most one sortie",
        })

    _write_csv(output_dir / "problem1_swap_sensitivity_k4_reference.csv", rows)
```

- [ ] **Step 7: 在 `run_all_experiments` 中调用 K=4 参考表**

```python
_run_problem1_swap_sensitivity(data, output_dir)
_run_problem1_swap_sensitivity_k4_reference(data, output_dir)
```

## 4. 测试

- [ ] **Step 8: 更新输出存在性测试**

文件：`tests/test_experiments.py`

```python
assert (tmp_path / "problem2_baseline_comparison.csv").exists()
assert (tmp_path / "problem1_swap_sensitivity_k1.csv").exists()
assert (tmp_path / "problem1_swap_sensitivity_k4_reference.csv").exists()
```

- [ ] **Step 9: 新增基准对比数值测试**

```python
def test_problem2_baseline_comparison_base_only_row(tmp_path):
    run_all_experiments(DATA_PATH, tmp_path)
    with (tmp_path / "problem2_baseline_comparison.csv").open("r", encoding="utf-8", newline="") as handle:
        rows = list(csv.DictReader(handle))

    base = rows[0]
    assert base["scheme"] == "仅基础巡检(alpha->inf)"
    assert float(base["uav_phase_time_s"]) == pytest.approx(639.3318181818182)
    assert float(base["ground_review_time_s"]) == pytest.approx(3894.0)
    assert float(base["closed_loop_time_s"]) == pytest.approx(4533.331818181819)
    assert int(base["manual_count"]) == 16
    assert int(base["weighted_manual_cost"]) == 36
```

需要导入：

```python
import pytest
```

- [ ] **Step 10: 新增 K=1 换电敏感性测试**

```python
def test_problem1_swap_sensitivity_k1_uses_critical_path(tmp_path):
    run_all_experiments(DATA_PATH, tmp_path)
    with (tmp_path / "problem1_swap_sensitivity_k1.csv").open("r", encoding="utf-8", newline="") as handle:
        rows = list(csv.DictReader(handle))

    expected = {
        0: 988.3000000000001,
        150: 1138.3000000000002,
        300: 1288.3000000000002,
        450: 1438.3000000000002,
        600: 1588.3000000000002,
    }
    for row in rows:
        swap = int(row["battery_swap_time_s"])
        assert int(row["k"]) == 1
        assert float(row["uav_phase_time_s"]) == pytest.approx(expected[swap])
```

## 5. 论文同步

- [ ] **Step 11: 新增问题2基准小节**

位置：`report/c_uav_inspection_paper.md` 的问题2结果部分。

标题：

```markdown
## 6.X 问题2(1)：仅基础巡检与联合优化对比
```

表格：

```markdown
表X 仅基础巡检与联合优化方案对比

| 方案 | $T_u$/s | $T_g$/s | $T$/s | 人工点数 | 加权人工代价 $C_M$ |
|---|---:|---:|---:|---:|---:|
| 仅基础巡检($\alpha\to\infty$) | 639.33 | 3894.00 | 4533.33 | 16 | 36 |
| 推荐方案($K=4,\alpha=1.0$) | 从CSV读取 | 从CSV读取 | 从CSV读取 | 从CSV读取 | 从CSV读取 |
```

推荐方案行必须从 `problem2_baseline_comparison.csv` 第二行读取，不手抄旧数值。

- [ ] **Step 12: 替换换电敏感性小节**

旧标题：

```markdown
## 6.2 换电时间敏感性
```

改为：

```markdown
## 6.2 换电时间敏感性（K=1）
```

正文必须表达：

```markdown
在 $K=1$ 时，两趟基础巡检由同一架无人机连续执行，换电时间进入关键路径。因此 $\tau_b$ 每增加 150 s，$T_u$ 也增加 150 s。原 $K=4$ 情况下每架无人机最多执行一趟，换电不进入关键路径，只能作为补充说明，不能作为换电时间无影响的结论。
```

## 6. 验收命令

运行：

```bash
.venv/bin/python -m pytest tests/test_experiments.py -q
```

期望：

```text
all selected tests passed
```

检查输出：

```bash
cat outputs/c_uav_inspection/problem2_baseline_comparison.csv
cat outputs/c_uav_inspection/problem1_swap_sensitivity_k1.csv
cat outputs/c_uav_inspection/problem1_swap_sensitivity_k4_reference.csv
```

必须能看到：

```text
仅基础巡检(alpha->inf)
推荐方案(K=4, alpha=1.0)
```

